# Copy to .env if you want to override default compose values.

# Password for the bundled PostgreSQL database (defaults to ofc-demo-password).
# The API connects to the internal `db` service automatically, so there is no
# external connection string to configure for local/docker-compose runs.
DB_PASSWORD=ofc-demo-password
